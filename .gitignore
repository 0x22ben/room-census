# secrets and local files, never published
*.pem
.env
passphrase.txt
state.json
proofs.jsonl
census.jsonl
*.key
deploy_key*
__pycache__/
*.pyc
# runtime files of the census and of manual messages (server side, never published)
nonces.json
pending_publication.json
say_pending.json
census.lock
abandoned/
# deployment manifest of the running copy: built from an approved commit, never committed with it
deploy_manifest.json

# editor and tool settings that belong to one machine
web/.claude/
