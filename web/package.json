{
  "name": "room-census-web",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "stage": "node scripts/stage-public-data.mjs",
    "build": "node scripts/stage-public-data.mjs && astro build",
    "preview": "astro preview",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "astro": "7.3.3"
  }
}
